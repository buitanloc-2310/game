{
  "name": "sky-first-games",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "typecheck": "tsc --noEmit",
    "deploy:worker": "npm run build && wrangler deploy --config wrangler.worker.jsonc",
    "db:migrate": "wrangler d1 migrations apply GAME_DB --remote --config wrangler.worker.jsonc",
    "db:migrate:local": "wrangler d1 migrations apply GAME_DB --local --config wrangler.worker.jsonc"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "wrangler": "^4.31.0"
  }
}

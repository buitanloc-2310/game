import React from 'react';
import { ArrowLeft, Mail, LockKeyhole, UserRoundPlus } from 'lucide-react';
import { api, authToken } from '../lib/api';
import { navigate } from '../lib/router';
import type { Account } from '../types';
export function InitializePage({onAuth}:{onAuth:(account:Account)=>void}){const [email,setEmail]=React.useState(''),[password,setPassword]=React.useState(''),[displayName,setDisplayName]=React.useState(''),[msg,setMsg]=React.useState(''),[busy,setBusy]=React.useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setMsg('');try{const r=await api.register(email.trim(),password,displayName.trim());authToken.set(r.token);onAuth(r.account);navigate('/creator')}catch(e:any){setMsg(e.message)}finally{setBusy(false)}};return <main className="auth-page auth-v2"><button className="auth-back" onClick={()=>navigate('/login')}><ArrowLeft/>Sign in</button><section className="auth-card auth-card-v2"><div className="auth-brand"><img src="/brand/sky-first-logo.png" alt="Sky First"/><div><strong>Sky First Games</strong><small>Self Initialization</small></div></div><span className="eyebrow">ACCOUNT INITIALIZATION</span><h1>Create your account</h1><p>Each email can create one account. Admin permission is granted separately after account creation.</p><form onSubmit={submit}><label><span><UserRoundPlus/>Display name</span><input required value={displayName} onChange={e=>setDisplayName(e.target.value)}/></label><label><span><Mail/>Email</span><input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label><span><LockKeyhole/>Password</span><input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label>{msg&&<div className="error-box">{msg}</div>}<button className="primary-button full big" disabled={busy}>{busy?'Processing…':'Create Account'}</button></form></section></main>}

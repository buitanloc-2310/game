import React from 'react';
import { KeyRound, Link2, UserRound } from 'lucide-react';
import { navigate } from '../lib/router';
export function JoinPage(){const preset=new URLSearchParams(location.search).get('code')||'';const [code,setCode]=React.useState(preset);const [name,setName]=React.useState('');return <main className="page-shell narrow"><div className="page-hero compact"><span className="eyebrow">JOIN GAME</span><h1>Nhập mã để tham gia</h1><p>Học viên không cần tài khoản. Chỉ cần mã/link từ TNV hoặc người tạo trò chơi.</p></div><div className="panel join-panel"><label><span><KeyRound/>Mã trò chơi</span><input inputMode="numeric" maxLength={8} placeholder="482 731" value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,''))}/></label><label><span><UserRound/>Tên hiển thị</span><input placeholder="Tên của bạn" value={name} onChange={e=>setName(e.target.value)}/></label><button className="primary-button big full" disabled={!code||!name} onClick={()=>navigate(`/room?code=${encodeURIComponent(code)}&name=${encodeURIComponent(name)}`)}>Tham gia</button><div className="join-help"><Link2 size={16}/>Nếu bạn có link trực tiếp, chỉ cần mở link — không cần nhập lại mã.</div></div></main>}

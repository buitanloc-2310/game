import React from 'react';
import { Menu, X, LogIn, Wrench, ShieldCheck } from 'lucide-react';
import { navigate } from '../lib/router';
import type { Account, SiteConfig } from '../types';
export function NavBar({account,onLogout,config}:{account:Account|null;onLogout:()=>void;config:SiteConfig}){const [open,setOpen]=React.useState(false);const go=(p:string)=>{setOpen(false);navigate(p)};return <header className="topbar"><div className="topbar-inner"><button className="brand-button brand-with-logo" onClick={()=>go('/')} aria-label="Trang chủ"><img src={config.logoUrl||'/brand/sky-first-logo.png'} alt="Sky First"/><span className="brand-text"><strong>SKY FIRST</strong><em>GAMES</em></span></button><nav className={open?'nav-links open':'nav-links'}>{(config.navigationItems?.length?config.navigationItems:[{label:'Khám phá',url:'/explore'},{label:'Nhập mã',url:'/join'},{label:'Chơi nhanh',url:'/quick-play'}]).map(item=>item.url.startsWith('/')?<button key={item.url} onClick={()=>go(item.url)}>{item.label}</button>:<a key={item.url} href={item.url} target="_blank" rel="noreferrer">{item.label}</a>)}{account&&<button onClick={()=>go('/creator')}><Wrench/>Creator</button>}{account?.isAdmin&&<button onClick={()=>go('/admin')}><ShieldCheck/>Admin</button>}</nav><div className="nav-actions">{account?<><button className="account-pill" onClick={()=>go('/creator')}>{account.displayName}</button><button className="ghost-button small" onClick={onLogout}>Đăng xuất</button></>:<button className="ghost-button" onClick={()=>go('/login')}><LogIn/>Creator</button>}<button className="menu-button" onClick={()=>setOpen(v=>!v)}>{open?<X/>:<Menu/>}</button></div></div></header>}
